@model IEnumerable<AlphaScheduler.Models.Profesor>

@{
    ViewBag.Title = "Profesor";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Profesor
            <small>Control panel</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Forms</li>
        </ol>
    </section>


    <!-- Main content -->
    <section class="content">


        <div class="box box-primary">

            <div class="box-header with-border">
                <button class="btn btn-primary" onclick="location.href='@Url.Action("Create", "Profesors")';return false;"><h3 class="box-title">Create New</h3></button>
            </div><!-- /.box-header -->
            <h3 class="page-header box-title text-center">List of Profesor</h3>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Email)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.FK_Id_Sede)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.FK_Id_Sede)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id_Profesor }) |
                            @Html.ActionLink("Details", "Details", new { id = item.Id_Profesor }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id_Profesor })
                        </td>
                    </tr>
                }

            </table>
        </div><!-- /.box -->
    </section><!-- /.content -->

</div><!-- /.content-wrapper -->